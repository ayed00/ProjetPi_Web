{% extends 'base.html.twig' %}

{% block title %}EventsReservation{% endblock %}

{% block body %}
    <h1>EventsReservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_reservation.id }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ events_reservation.Tel }}</td>
            </tr>
            <tr>
                <th>Reservations</th>
                <td>{{ events_reservation.Reservations }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('events_reservation_index') }}">back to list</a>

    <a href="{{ path('events_reservation_edit', {'id': events_reservation.id}) }}">edit</a>

    {{ include('events_reservation/_delete_form.html.twig') }}
{% endblock %}
